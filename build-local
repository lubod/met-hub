# export ENV=dev
# export NODE_OPTIONS="--max-old-space-size=4096"
time node esbuild.mjs && time sudo docker-compose -f docker-compose-local.yml build && time sudo docker-compose -f docker-compose-local.yml up
